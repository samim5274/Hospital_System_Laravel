// $card = session()->get('cart');
        // $card[$id] = [
        //     "id" => $dataTest->id,
        //     "name" => $dataTest->name,
        //     "catid" => $dataTest->catid,
        //     "price" => $dataTest->price,
        //     "room" => $dataTest->room
        // ];
        // session()->put('cart', $card);
        // return redirect()->back()->with('success', 'Test added to cart successfully!');

